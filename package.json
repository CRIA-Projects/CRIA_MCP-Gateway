{
  "name": "cria-mcp-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "A modular, stateless MCP gateway MVP.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/platform/http/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/platform/http/server.js",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3",
    "@supabase/supabase-js": "^2.116.0"
  }
}
